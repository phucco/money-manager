<?php
defined('BASEPATH') OR exit('No direct script access allowed');


/*function site_assets($url = '')
{
	if (strpos($url, '/') == 0)
	{
	 	return base_url('site/' . substr($url, 1));
	}
	else
	{
		return base_url('site/' . $url);
	}
}

function upload_assets($url = '')
{
	if (strpos($url, '/') == 0)
	{
	 	return base_url('upload/' . substr($url, 1));
	}
	else
	{
		return base_url('upload/' . $url);
	}
}

function admin_assets($url = '')
{
	if (strpos($url, '/') == 0)
	{
	 	return base_url('adm/' . substr($url, 1));
	}
	else
	{
		return base_url('adm/' . $url);
	}
}

function admin_url($url = '')
{
	if ( empty($url) ) return base_url('admin/dashboard');
	
	return base_url('admin/' . $url);
	
}
*/